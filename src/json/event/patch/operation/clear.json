{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Clear operation",
  "description": "An operation to clear the content of an array, map or property.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "type",
    "path"
  ],
  "properties": {
    "type": {
      "description": "The operation type identifier.",
      "const": "clear"
    },
    "path": {
      "description": "The path where to clear the content.",
      "$ref": "../pointer.json"
    }
  }
}
