{
  "$id": "https://schema.croct.com/json/template/action/install.json",
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "title": "Install",
  "description": "Run the package installation process, which is typically used to install dependencies.",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "description": "The name of the action.",
      "const": "install"
    }
  },
  "required": ["name"],
  "additionalProperties": false,
  "examples": [
    {
      "name": "install"
    }
  ]
}
