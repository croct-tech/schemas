{
  "schema": "event/model/order.json",
  "input": {},
  "errors": [
    "The object must have a property whose name is \"orderId\".",
    "The object must have a property whose name is \"currency\".",
    "The object must have a property whose name is \"items\".",
    "The object must have a property whose name is \"total\"."
  ]
}
