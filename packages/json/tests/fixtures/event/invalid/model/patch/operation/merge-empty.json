{
  "schema": "event/model/patch/operation/merge.json",
  "input": {},
  "errors": [
    "The object must have a property whose name is \"type\".",
    "The object must have a property whose name is \"path\".",
    "The object must have a property whose name is \"value\"."
  ]
}
