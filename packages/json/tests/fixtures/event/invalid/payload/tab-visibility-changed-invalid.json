{
  "schema": "event/payload/tab-visibility-changed.json",
  "input": {
    "type": "tabVisibilityChanged",
    "tabId": 1,
    "visibility": 3,
    "additionalProperty": "foo"
  },
  "errors": [
    "[/tabId] The value must be of string type, but actual type is integer.",
    "[/visibility] The value must be of string type, but actual type is integer.",
    "[/visibility] The value must be one of [\"visible\", \"hidden\"].",
    "[/additionalProperty] The object must not have a property whose name is \"additionalProperty\"."
  ]
}
