{
  "schema": "event/payload/page-loaded.json",
  "input": {},
  "errors": [
    "The object must have a property whose name is \"type\".",
    "The object must have a property whose name is \"url\".",
    "The object must have a property whose name is \"title\".",
    "The object must have a property whose name is \"lastModifiedTime\"."
  ]
}
