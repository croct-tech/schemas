{
  "schema": "event/payload/user-signed-out.json",
  "input": {
    "type": "userSignedOut",
    "externalUserId": 9,
    "additionalProperty": "foo"
  },
  "errors": [
    "[/externalUserId] The value must be of string type, but actual type is integer.",
    "[/additionalProperty] The object must not have a property whose name is \"additionalProperty\"."
  ]
}
